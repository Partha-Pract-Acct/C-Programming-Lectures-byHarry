/* write a program to take string as an input from the user %s confirm that the string is equal */

#include <stdio.h>

int main()
{
    char str[7];

    printf("Enter: ");

    scanf("%s", str);
    printf("%s", str);
    return 0;
}