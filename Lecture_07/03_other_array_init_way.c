/* Other way to init the values of array */

#include <stdio.h>

int main()
{
    int arr[] = {12, 13, 16, 14};

    for (int i = 0; i < 4; i++)
    {
        printf("The values are: %d\n", arr[i]);
    }

    return 0;
}